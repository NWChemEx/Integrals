// #pragma once
// #include <chemist/basis_set/ao_basis_set.hpp>
// #include <property_types/types.hpp>
// #include <sde/types.hpp>

// namespace integrals::type {

// /// We use the same types as property types
// using namespace property_types::type;

// /// Type of th object holding parameters for Gaussian AOs
// template<typename T>
// using basis_set = chemist::AOBasisSet<T>;

// // A vector whose elements are sizes
// using size_vector = std::vector<type::size>;

// // A vector of pairs, where each pair is a size
// using pair_vector = std::vector<std::pair<type::size, type::size>>;

// } // namespace integrals::type
