#pragma once
#include "integrals/property_types.hpp"
#include <pluginplay/module_base.hpp>

// namespace integrals {

// template<typename PropType>
// DECLARE_MODULE(CauchySchwarzScreened);

// /// Typedefs of the modules
// ///@{
// template<typename T>
// using ScreenedERI3C = CauchySchwarzScreened<pt::eri3c<T>>;

// template<typename T>
// using ScreenedERI4C = CauchySchwarzScreened<pt::eri4c<T>>;

// template<typename T>
// using ScreenedSTG3C = CauchySchwarzScreened<pt::stg3c<T>>;

// template<typename T>
// using ScreenedSTG4C = CauchySchwarzScreened<pt::stg4c<T>>;

// template<typename T>
// using ScreenedYukawa3C = CauchySchwarzScreened<pt::yukawa3c<T>>;

// template<typename T>
// using ScreenedYukawa4C = CauchySchwarzScreened<pt::yukawa4c<T>>;
// ///@}

// extern template class CauchySchwarzScreened<pt::eri3c<double>>;
// extern template class CauchySchwarzScreened<pt::eri4c<double>>;
// extern template class CauchySchwarzScreened<pt::stg3c<double>>;
// extern template class CauchySchwarzScreened<pt::stg4c<double>>;
// extern template class CauchySchwarzScreened<pt::yukawa3c<double>>;
// extern template class CauchySchwarzScreened<pt::yukawa4c<double>>;

} // namespace integrals
